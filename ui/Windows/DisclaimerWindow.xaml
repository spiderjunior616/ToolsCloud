<ui:FluentWindow x:Class="ToolsCloud.Windows.DisclaimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:res="clr-namespace:ToolsCloud.Resources"
        Title="{res:Loc Disclaimer_WindowTitle}"
        Height="720" Width="580"
        MinHeight="720" MinWidth="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{res:Loc Disclaimer_WindowTitle}" ShowMinimize="False" ShowMaximize="False" />

        <Border Grid.Row="1"
                BorderBrush="#CC2222"
                BorderThickness="2"
                Margin="16,0,16,16"
                CornerRadius="8"
                Padding="0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#CC2222"
                        CornerRadius="6,6,0,0"
                        Padding="16,12">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{res:Loc Disclaimer_BuildBanner}"
                                   FontFamily="Cascadia Code,Consolas,Courier New"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,4" />
                        <TextBlock Text="{res:Loc Disclaimer_ExperimentalBanner}"
                                   FontFamily="Cascadia Code,Consolas,Courier New"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="#FFCCCC"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              Padding="24,16,24,8">
                    <StackPanel>

                        <TextBlock x:Name="AsciiArt"
                                   FontFamily="Cascadia Code,Consolas,Courier New"
                                   FontSize="13"
                                   Foreground="#CC2222"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"
                                   Opacity="0"
                                   Text="  XX      XX     XX      XX     XX      XX&#xA;   XX    XX       XX    XX       XX    XX&#xA;    XX  XX         XX  XX         XX  XX&#xA;     XXXX           XXXX           XXXX&#xA;    XX  XX         XX  XX         XX  XX&#xA;   XX    XX       XX    XX       XX    XX&#xA;  XX      XX     XX      XX     XX      XX" />

                        <Border Background="#1A0000"
                                BorderBrush="#CC2222"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="20,16"
                                Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock FontSize="13"
                                           Foreground="#FF4444"
                                           TextWrapping="Wrap"
                                           LineHeight="22">
                                    <Run FontWeight="Bold" Text="{res:Loc Disclaimer_WarningBold}" />
                                    <LineBreak /><LineBreak />
                                    <Run Text="{res:Loc Disclaimer_ItMay}" />
                                    <LineBreak /><LineBreak />
                                    <Run Text="    " /><Run FontWeight="Bold" Text="{res:Loc Disclaimer_Corrupt}" /><Run Text="{res:Loc Disclaimer_CorruptSuffix}" />
                                    <LineBreak />
                                    <Run Text="    " /><Run FontWeight="Bold" Text="{res:Loc Disclaimer_Lose}" /><Run Text="{res:Loc Disclaimer_LoseSuffix}" />
                                    <LineBreak />
                                    <Run Text="    " /><Run FontWeight="Bold" Text="{res:Loc Disclaimer_Overwrite}" /><Run Text="{res:Loc Disclaimer_OverwriteSuffix}" />
                                    <LineBreak /><LineBreak />
                                    <Run FontWeight="Bold" FontSize="13" Text="{res:Loc Disclaimer_BackUpBold}" />
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   FontSize="12.5"
                                   TextWrapping="Wrap"
                                   LineHeight="20"
                                   Margin="0,0,0,8">
                            <Run Text="{res:Loc Disclaimer_ExplanationText}" />
                        </TextBlock>

                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        CornerRadius="0,0,6,6"
                        Padding="24,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="CountdownText"
                                   Grid.Column="0"
                                   Text="{res:Loc Disclaimer_PleaseReadCarefully}"
                                   Foreground="#FF4444"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />

                        <ui:Button x:Name="CancelButton"
                                   Grid.Column="1"
                                   Content="{res:Loc Disclaimer_Cancel}"
                                   Margin="0,0,8,0"
                                   Click="Cancel_Click" />

                        <ui:Button x:Name="AcceptButton"
                                   Grid.Column="2"
                                   Content="{res:Loc Disclaimer_AcceptButton}"
                                   Appearance="Danger"
                                   IsEnabled="False"
                                   Click="Accept_Click" />
                    </Grid>
                </Border>

            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
